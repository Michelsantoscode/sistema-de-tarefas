CREATE TABLE tarefa (
                        id BIGINT NOT NULL AUTO_INCREMENT,
                        titulo VARCHAR(20) NOT NULL,
                        descricao VARCHAR(40) NOT NULL,
                        PRIMARY KEY (id)
);